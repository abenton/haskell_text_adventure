{-# OPTIONS -Wall -fwarn-tabs -fno-warn-type-defaults #-}

module Clients where

-- | Contains definitions for clients along with some simple behaviors/ways of
-- | composing behaviors.

annoyingNPCThread :: IO ()
annoyingNPCThread = error "undefined"
